{
    "name": "blockchain",
    "version": "1.0.0",
    "description": "Zero-Day Sentinel Blockchain Integration",
    "scripts": {
      "compile": "hardhat compile",
      "deploy": "hardhat run scripts/deploy.js --network localhost"
    },
    "devDependencies": {
      "hardhat": "^2.12.0",
      "@nomiclabs/hardhat-ethers": "^2.0.0",
      "ethers": "^5.0.0"
    }
  }
  